# EZChess Web 使用說明

本專案為**獨立專案**，與「全民查證公社」畢業專題前端無關，請單獨複製或開啟本資料夾 `EZChess-web` 使用。

## 環境

- [Node.js](https://nodejs.org) LTS（含 npm）
- 現代瀏覽器（Chrome、Edge 等）

## 開發執行

在 `EZChess-web` 根目錄：

```bash
npm install
npm run dev
```

依終端機提示開啟網址。注意：**畢業專題前端預設也是 `http://localhost:5173`**，若兩邊同時開發會衝突。請關掉其中一個，或在本專案改用：`npm run dev:safe`（固定 **5199** 埠）。

## 專案根目錄捷徑（Windows，不寫入桌面）

請用腳本把捷徑建在 **`EZChess-web` 專案根目錄**（與 `package.json` 同一層），檔名為 `EZChess.lnk`。

例如：`...\新增資料夾\EZChess-web\EZChess.lnk`。

### 方法一：建立捷徑檔

1. 開啟資料夾：`EZChess-web\scripts`
2. Shift + 右鍵 →「在此處開啟 PowerShell 視窗」
3. 執行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
.\Create-Desktop-Shortcut.ps1
```

4. 回到 **`EZChess-web` 根目錄** 可看到 **EZChess** 捷徑（`EZChess.lnk`）。雙擊後會建置並啟動預覽（**http://127.0.0.1:4188/**，與畢業專題埠位不同），關閉黑色命令視窗即停止。

捷徑會指向根目錄的 **`EZChess-Start.bat`**（勿刪除）。若捷徑仍無反應，請改雙擊 **`EZChess-Start.bat`** 測試；若可開啟視窗，請刪除舊的 `EZChess.lnk` 後再執行一次本節步驟 3 重建捷徑。

### 方法二：直接啟動

雙擊 **`EZChess-Start.bat`**（專案根目錄），或雙擊 `scripts\Launch-EZChess.bat`（效果同上）。

### 若無法執行 .ps1

僅本次工作階段放寬：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
```

再執行 `Create-Desktop-Shortcut.ps1`。

## 棋戲操作摘要

- **開局**：標準／範例棋形／貼上 8 行文字棋盤。
- **UV 調整**：限時 120 秒，任一枚子移到任一空格一次，或跳過。
- **行棋**：AB（紅）先手；點己方子再點目標格；可手動輸入 `B:(3,5)-(2,4)` 格式。
- **計時**：前四手每步 120 秒；之後每步 60 秒，可延長至 120 秒（每方 3 次）。
- **終局**：最多 40 手，比獵殺分，同分比累計用時。

## 常見問題

**出現「'd' 不是內部或外部命令」或亂碼拆成指令**  
Windows 的 `cmd` 會用系統預設編碼（繁中多為 Big5）讀取 `.bat`。**請勿把中文寫進批次檔**。目前 **`EZChess-Start.bat`** 只負責呼叫 **`Start-EZChess.ps1`**（PowerShell，較適合中文路徑與 UTF-8）；實際安裝與 `npm run desktop` 都在 `.ps1` 內執行。

**視窗仍一閃即關**  
請改在 `EZChess-web` 根目錄於 PowerShell 執行：`powershell -NoProfile -ExecutionPolicy Bypass -File .\Start-EZChess.ps1`，將畫面訊息截圖或複製。

**結束代碼 -1073740791（0xC0000409）**  
少數環境下 **`vite preview`** 會在啟動後立刻崩潰。`npm run desktop` 已改為 **`vite build` 後用靜態伺服器 `serve`**（不走 Vite 預覽），通常可避開此問題。請在終端機出現 **`Accepting connections`**（或含 **4188**）後**手動開瀏覽器**。

**瀏覽器還是全民查證公社（畢業專題）**  
代表你開到**畢業專題的網址**（常見 **`http://localhost:5173/`**）。EZChess 預覽請開：**`http://127.0.0.1:4188/`** 或 **`http://localhost:4188/`**（埠 **4188**）。請勿沿用 5173 書籤。

**出現「拒絕連線」**  
請先確認黑色視窗裡 **`serve` 已印出 `Accepting connections`（埠 4188）** 再開瀏覽器；若沒有這行代表伺服器沒起來（請看上方錯誤）。`serve` 已綁定 **`0.0.0.0:4188`**（本機與 localhost 皆可）。若仍拒絕，請關閉會占用 **4188** 的其他程式後重跑 `npm run desktop`。

**捷徑無效／專案搬移過**  
捷徑指向的 `Launch-EZChess.bat` 路徑若已變更，請在**新位置**的 `scripts` 資料夾重新執行 `Create-Desktop-Shortcut.ps1`（會在專案根目錄重新產生 `EZChess.lnk`）。

**埠號 4188 被占用**  
請關閉占用程式，或修改 `package.json` 內 `preview`／`desktop` 腳本中的 **`4188`**（與本說明中的網址埠號一致）。

**遠端對弈**  
本版為同機雙人，不含線上連線。
