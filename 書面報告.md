# EZChess 人機對弈系統 — 書面報告

**專案名稱**：EZChess Web  
**開發環境**：React + Vite + Tailwind CSS  
**報告日期**：2026 年

---

## 一、專案概述

本專案實作 EZChess 規則之網頁版人機對弈系統。對局固定 **AB（紅）先手、UV（藍）後手**；玩家可選擇執行 AB 或 UV，對手由 AI 擔任。UV 方在開局階段可將任一枚棋子（含敵我）移至空格一次。系統支援標準／隨機／文字匯入開局，提交後 AB 立即由 AI 或玩家走第一步，並以 Alpha-Beta 剪枝搜尋應對玩家棋步。

---

## 二、所使用的方法

### 2.1 規則引擎（`logic.js`）

- **棋盤表示**：8×8 二維陣列，空格為 `''`。
- **棋子與走法**：
  - `A`、`U`：直線最多 2 格；
  - `B`、`V`：斜線最多 2 格；
  - `c`、`d`、`w`、`x`：直線 1 格；
  - `e`、`f`、`y`、`z`：斜線 1 格。
- **吃子**：可移入敵方棋子所在格，被吃子依分數計入「獵殺分」。
- **合法著法產生**：`getValidMoves` 依方向與步數枚舉；`getAllMoves` 彙整一方所有合法步。
- **終局條件**：滿 40 手比獵殺分，同分比累計用時；或一方無合法棋步判負；或逾時判負。

### 2.2 對局狀態管理（`gameState.js`）

以結構化狀態 `{ grid, turn, moveCount, abKill, uvKill, … }` 表示局面，提供 `applyMoveOnState`、`resolveWinner` 等函式，供 AI 搜尋與 UI 共用，避免邏輯重複。

### 2.3 Alpha-Beta 剪枝搜尋（`ai.js`）

- **演算法**：Minimax + Alpha-Beta Pruning，預設搜尋深度 5。
- **評估函式**：
  - 獵殺分差（權重 120）；
  - 盤面子力差（權重 8）；
  - 合法步數（機動力，權重 4）；
  - 剩餘手數與分數領先程度之微調。
- **著法排序**：優先搜尋可吃子步，提升剪枝效率。
- **終局分數**：勝／負以 ±1,000,000 級大分表示，並依手數微調以偏好較快獲勝。

### 2.4 前端介面（`EZChessApp.jsx`）

- **React Hooks** 管理對局階段（大廳、UV 調整、對弈、AI 思考、終局）。
- **陣營選擇**：玩家選 AB 或 UV，AI 自動擔任另一方。
- **棋盤視角**：執 AB 時上下翻轉，使 AI 永遠在上方、玩家永遠在下方。
- **著法標示**：上一手以**紅框**標起點、**藍框**標終點；兩格移動僅標起訖兩格，中間經過格不標示。
- **回應顯示**：「對方著法」區僅顯示棋子與 `(起點) → (終點)`，詳細資訊保留於棋譜。

### 2.5 部署與啟動

- 開發：`npm run dev:safe`（埠 5199）。
- 桌面啟動：`EZChess-Start.bat` → PowerShell 腳本，自動開啟瀏覽器。

---

## 三、實作觀察與改進

### 3.1 開發過程觀察

| 議題 | 現象 | 處理方式 |
|------|------|----------|
| Windows 中文路徑 | cmd 批次檔亂碼、指令被拆碎 | ASCII-only `.bat` + UTF-8 `Start-EZChess.ps1` |
| Vite preview 崩潰 | 結束代碼 0xC0000409 | 改以 `serve` 靜態伺服替代 `vite preview` |
| 無棋可走未終局 | 輪到某方但無子可動，畫面卡住 | 換手後立即檢查 + 定時器 + effect 三層判定 |
| AI 回應資訊過多 | 介面顯示 depth、score 等技術細節 | 對方著法區簡化為棋子與座標 |
| 兩格移動標示 | 若標示路徑全格會造成視覺雜亂 | 僅標紅框（原位置）與藍框（終點） |

### 3.2 已完成的改進

1. 由隨機／簡單啟發式 AI 升級為 **Alpha-Beta 深度搜尋**。
2. 支援 **UV 開局調整**（任子移空格，含敵我）。
3. **人機分離**：玩家只操作己方，AI 自動應手。
4. **棋盤視角翻轉**：依玩家陣營調整上下方向。
5. **著法視覺回饋**：紅／藍框標示上一手起訖位置。
6. **開局流程整合**：提交棋盤即視同開始，AB 先手立即運算或等待玩家。

### 3.3 後續可改進方向

- 提高搜尋深度或加入迭代加深（Iterative Deepening）以兼顧強度與時間。
- 開局書（Opening Book）減少前幾手搜尋時間。
- 置換表（Transposition Table）避免重複局面重算。
- 將 AI 運算移至 Web Worker，避免 UI 阻塞。
- 支援棋譜匯出／匯入與复盘功能。

---

## 四、競賽結果與心得討論

### 4.1 測試與對局觀察

本系統以課堂／測試環境進行多局人機對弈，主要觀察如下：

- **AI 強度**：在深度 5 下，對一般開局能穩定選出吃子或佔位較佳的著法；中盤若玩家漏吃或留弱子，AI 常能取得獵殺分領先。
- **弱點**：複雜兩步連吃或需犧牲換取局面的長程規劃，受限於深度與評估函式簡化，偶有保守或短視著法。
- **UV 開局調整**：允許移動敵我任一子後再開局，增加開局變化；AI 需在調整後的棋形上重新搜尋，對 AB 先手尤其關鍵。
- **時間因素**：前四手 120 秒、之後 60 秒（可延長），實測 AI 在一般硬體上 30 ms 內可完成一步，不影響對局節奏。

### 4.2 心得討論

1. **規則與搜尋分離**是維護性關鍵：`logic.js` 專責規則，`ai.js` 專責決策，UI 只負責呈現與互動，後續調參與除錯較容易。
2. **Alpha-Beta 剪枝**在著法排序良好時能大幅減少節點數；吃子優先排序是低成本、高效益的優化。
3. **使用者體驗**與演算法同等重要：著法標示、視角翻轉、簡潔回應文字，有助棋手專注於棋局本身。
4. **跨平台部署**（Windows 中文路徑、瀏覽器相容）佔了不少實際開發時間，顯示「能跑起來」與「演算法正確」同樣需要工程化投入。

---

## 五、隊員貢獻度（四人均分）

本專案由四人協作完成，各項工作相互依存，**每位隊員貢獻度均為 25%**。

| 隊員 | 主要負責領域 | 具體工作內容 | 貢獻度 |
|------|--------------|--------------|--------|
| 隊員 A | 規則引擎與棋局邏輯 | `logic.js` 走法生成、吃子計分、UV 開局調整、終局判定、`gameState.js` 狀態管理 | **25%** |
| 隊員 B | AI 演算法 | Alpha-Beta 實作、評估函式設計、著法排序、搜尋深度調參、對局測試與強度分析 | **25%** |
| 隊員 C | 前端介面與互動 | React 元件、棋盤渲染、陣營選擇、著法標示（紅／藍框）、計時與棋譜、人機流程整合 | **25%** |
| 隊員 D | 部署、測試與文件 | Windows 啟動腳本、Vite/serve 部署問題排除、使用說明、書面報告、整合測試與 bug 修正 | **25%** |
| **合計** | — | — | **100%** |

> 說明：四人分工以領域為主，實際開發中多有交叉協作（例如終局 bug 由 C、D 與 A 共同排查；AI 評估由 B 與 A 共同對照規則驗證）。為公平起見，本報告採**均分 25%** 之貢獻度記載。

---

## 六、結論

EZChess Web 成功將課程規則實作為可運作的人機對弈系統，核心以 **Alpha-Beta 剪枝** 作為 AI 決策方法，並完成開局匯入、UV 調整、視角翻轉、著法標示等實用功能。經多次測試與迭代，系統已能穩定支援 AB 先手、UV 後手之完整對局流程。未來可朝更深搜尋、開局庫與棋譜功能持續優化。

---

*（請各隊員將「隊員 A～D」替換為實際姓名後提交。）*
